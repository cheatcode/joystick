import component from './component';
import each from './each';
import i18n from './i18n';
import when from './when';

export default {
  c: component,
  component,
  e: each,
  each,
  i: i18n,
  i18n,
  w: when,
  when,
};